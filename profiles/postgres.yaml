# alc-postgres: postgresql-16 instance 用
# - pgdata volume は up.sh が `incus config device add` で instance ごとに足す
name: alc-postgres
description: incus-sandbox postgres profile (env only; volumes are added per-instance)
config:
  environment.PGDATA: /var/lib/postgresql/16/main
devices: {}
