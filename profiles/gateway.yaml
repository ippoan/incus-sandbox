# alc-gateway: gateway instance 用
# - src / target / cargo-cache + proxy device は up.sh が後付け
name: alc-gateway
description: incus-sandbox gateway profile (env only; devices added per-instance)
config:
  environment.RUST_LOG: "gateway=info,tower_http=info"
  environment.CARGO_HOME: /root/.cargo
  environment.PATH: /root/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
devices: {}
